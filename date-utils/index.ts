import { getDateAsString, getDateFromString } from "./date-parsing.date-utils.js";
import { isDateAfter, isDateBefore, isValidDate } from "./date-validation.date-utils.js";

export const dateUtils = {
  getDateAsString,
  getDateFromString,
  isValidDate,
  isDateBefore,
  isDateAfter,
};
